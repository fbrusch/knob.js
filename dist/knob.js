(function e(t,n,r){function s(o,u){if(!n[o]){if(!t[o]){var a=typeof require=="function"&&require;if(!u&&a)return a(o,!0);if(i)return i(o,!0);var f=new Error("Cannot find module '"+o+"'");throw f.code="MODULE_NOT_FOUND",f}var l=n[o]={exports:{}};t[o][0].call(l.exports,function(e){var n=t[o][1][e];return s(n?n:e)},l,l.exports,e,t,n,r)}return n[o].exports}var i=typeof require=="function"&&require;for(var o=0;o<r.length;o++)s(r[o]);return s})({1:[function(require,module,exports){
// Generated by CoffeeScript 1.6.3
var app;

app = angular.module('knob', []);

app.controller('knobCtrl', function($scope) {
  return $scope.value = 45;
});

app.directive('knob', function() {
  return {
    restrict: 'E',
    scope: {
      pos: "=?",
      max: "@?",
      min: "@?",
      height: "@?",
      width: "@?",
      color: "@?"
    },
    template: "\n<div style=\"height:{{height}}; width:{{width}}\">\n  <svg width=\"100%\" height=\"100%\" viewbox=\"0 0 100 100\" ng-mousedown=\"mousedown($event)\">\n    <circle cx=\"50\" cy=\"50\" r=\"45\" stroke-width=\"10\" stroke=\"{{color}}\" fill=\"white\"></circle>\n    <line x1=\"50\" y1=\"50\" x2=\"50\" y2=\"17\" stroke-width=\"10\" stroke=\"{{color}}\" transform=\"rotate({{normalise(pos)}} 50 50)\"></line>\n  </svg>\n</div>",
    controller: function($scope, $document) {
      $scope.color = $scope.color || "black";
      $scope.max = $scope.max || "100";
      $scope.min = $scope.min || "0";
      $scope.pos = $scope.pos || 0;
      $scope.height = $scope.pos || "100px";
      $scope.width = $scope.pos || "100px";
      $scope.normalise = function(v) {
        return (v - $scope.min) / ($scope.max - $scope.min) * 360;
      };
      return $scope.mousedown = function(event) {
        var inBounds, mouseGetXY, startPos, y;
        event.preventDefault();
        startPos = $scope.pos;
        y = event.y;
        inBounds = function(v) {
          return v <= $scope.max && v >= $scope.min;
        };
        mouseGetXY = function(e) {
          if (inBounds(startPos + e.y - y)) {
            $scope.pos = startPos + e.y - y;
            return $scope.$apply();
          }
        };
        $document[0].addEventListener('mousemove', mouseGetXY);
        return $document[0].addEventListener('mouseup', function() {
          return $document[0].removeEventListener('mousemove', mouseGetXY);
        });
      };
    }
  };
});

},{}]},{},[1]);
